# 지뢰찾기 AI — 로컬 GPU 개발 환경 (RTX 4060) 셋업 가이드

**버전**: 0.1
**작성일**: 2026년 06월 04일
**참고 문서**: 기술 백서 §3·§7, 파일 트리 §1·§3

> 구성 원칙: **Postgres만 Docker로 띄우고, Python(서버 + 트레이너)은 네이티브로 실행**한다. RTX 4060에 대한 GPU 접근을 가장 단순·깔끔하게 가져가기 위함이며, Docker GPU 패스스루(특히 Windows)의 번거로움을 피한다. 이 문서는 파일 트리 §3.1의 compose 스케치를 로컬 개발용으로 구체화한 것이다(풀 Docker+GPU 대안은 §11).

---

## 0. 구성 한눈에
- **DB**: Docker 컨테이너 1개(Postgres). 가볍고 격리됨.
- **서버·학습**: 로컬 venv + CUDA PyTorch 네이티브. GPU 직접 사용.
- **프론트(web)**: pnpm + Vite 네이티브.
- **기동 흐름**: `docker compose up -d db` → venv·의존성 → `alembic upgrade head` → uvicorn(서버) → 학습 시작.

---

## 1. 사전 요구사항
- **NVIDIA 드라이버**(최신 권장). `nvidia-smi`로 GPU·드라이버·지원 CUDA 버전 확인.
- **Python 3.11 또는 3.12**. (3.13은 PyTorch 휠 지원이 늦을 수 있어 3.11/3.12 권장)
- **Node 20+ / pnpm**.
- **Docker**(Postgres 컨테이너용).
- **CUDA Toolkit은 보통 불필요**: PyTorch 휠이 필요한 CUDA 런타임을 동봉한다. 드라이버만 맞으면 되고, 시스템 CUDA Toolkit은 소스 빌드/커스텀 확장 빌드 때만 필요.

---

## 2. PyTorch (CUDA) 설치 — 가장 중요한 단계
- 공식 셀렉터에서 명령을 생성한다: https://pytorch.org/get-started/locally/ (Stable / OS / Pip / CUDA 12.x).
- 4060은 Ada 아키텍처(compute capability 8.9)라 CUDA 12.x 휠과 호환된다. 셀렉터가 제시하는 **최신 12.x 태그**(cu124 / cu126 / cu128 등)를 쓰면 된다(보통 최신이 무난).
- 이 프로젝트는 이미지/오디오를 쓰지 않으므로 **torch만 설치**한다(torchvision·torchaudio 생략).
- 예시(셀렉터 명령을 우선하되, 형태는 이렇다):
  ```bash
  pip install torch --index-url https://download.pytorch.org/whl/cu124
  ```
- **재현성**: 버전이 정해지면 핀을 권장한다(예: `torch==2.x.x`). 핀 값은 셀렉터가 알려주는 현재 stable 버전으로.
- **주의**: 일반 requirements/pyproject에 `torch`를 그냥 넣고 PyPI에서 받으면 의도와 다른(또는 CPU) 빌드가 잡힐 수 있다. 그래서 **torch는 위 index-url로 따로 설치**하고, 나머지 의존성만 pyproject에 둔다(§6).

---

## 3. 설치 검증
```python
# server/verify_gpu.py 같은 임시 스크립트로 확인
import torch
print(torch.__version__)
print("CUDA available:", torch.cuda.is_available())   # True 여야 함
print("Device:", torch.cuda.get_device_name(0))       # 'NVIDIA GeForce RTX 4060 ...'
```
- `is_available()`가 False면: 드라이버↔휠 CUDA 불일치이거나 CPU 휠이 설치된 것 → §2 재설치.

---

## 4. `docker-compose.yml` (Postgres만)
> 레포 루트에 둔다. 서버/학습은 컨테이너에 넣지 않는다.
```yaml
# 로컬 개발용: Postgres만 컨테이너. 서버/학습은 네이티브 실행.
services:
  db:
    image: postgres:16
    container_name: msai-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-msai}
      POSTGRES_USER: ${POSTGRES_USER:-msai}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-msai} -d ${POSTGRES_DB:-msai}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
```

---

## 5. `.env.example`
> 레포 루트에 둔다. 복사해서 `.env`로 쓰고, `.env`는 git에 커밋하지 않는다.
```dotenv
# --- Database (compose의 db 서비스와 값 일치) ---
POSTGRES_DB=msai
POSTGRES_USER=msai
POSTGRES_PASSWORD=changeme
# SQLAlchemy(psycopg3) 연결 문자열
DATABASE_URL=postgresql+psycopg://msai:changeme@localhost:5432/msai

# --- Server ---
HOST=0.0.0.0
PORT=8000
# 콤마 구분. web(dev) 및 확장 출처 허용
CORS_ORIGINS=http://localhost:5173,chrome-extension://*

# --- Model storage ---
# 가중치(.pt/.onnx) 저장 경로(로컬). git 비커밋.
MODEL_STORAGE_DIR=./_models

# --- Training ---
MAX_CONCURRENT_RUNS=1
```

---

## 6. `server/pyproject.toml` (torch 제외 의존성)
> torch는 §2에서 별도 설치하므로 여기에 넣지 않는다.
```toml
[project]
name = "msai-server"
version = "0.1.0"
description = "Minesweeper AI training server (FastAPI + PyTorch)"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "websockets>=12",
  "sqlalchemy>=2.0",
  "alembic>=1.13",
  "psycopg[binary]>=3.2",
  "pydantic>=2.7",
  "pydantic-settings>=2.3",
  "numpy>=1.26",
  "onnx>=1.16",
  "python-dotenv>=1.0",
]
# torch는 CUDA 휠을 별도 설치(README §2):
#   pip install torch --index-url https://download.pytorch.org/whl/cu124

[project.optional-dependencies]
dev = ["ruff>=0.5", "pytest>=8.2", "httpx>=0.27"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["app*", "trainer*"]
```
> `app/`과 `trainer/`에 `__init__.py`를 두어 패키지로 인식되게 한다.

---

## 7. `server/Dockerfile` (선택, 배포용 API 서빙)
> 로컬 개발/학습은 네이티브가 기본. 이 이미지는 **학습 없이 API 서빙만** 할 때(예: 가벼운 호스트 배포) 쓴다. 추론은 클라이언트에서 ONNX로 하므로 **torch 미포함·CPU 베이스로 충분**하다. 학습 엔드포인트는 torch가 있는 네이티브 환경에서만 동작한다는 전제.
```dockerfile
# (선택) 배포용 API 서빙 이미지. 학습은 로컬 네이티브에서 수행.
FROM python:3.12-slim
WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
 && rm -rf /var/lib/apt/lists/*

COPY . .
RUN pip install --no-cache-dir .

EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

---

## 8. 실행 순서 (로컬)
```bash
# 0) 레포 루트에서 .env 준비
cp .env.example .env   # 값 확인/수정

# 1) DB 기동
docker compose up -d db

# 2) 파이썬 환경 (server/ 기준)
cd server
python -m venv .venv
# Windows:  .venv\Scripts\activate
# Linux/WSL: source .venv/bin/activate
pip install -e ".[dev]"                                          # pyproject 의존성
pip install torch --index-url https://download.pytorch.org/whl/cu124   # CUDA torch 별도(§2)

# 3) DB 마이그레이션 (alembic 설정은 M3 스캐폴딩에 포함)
alembic upgrade head

# 4) API 서버
uvicorn app.main:app --reload --port 8000

# 5) (다른 터미널) 학습 시작 — 직접 스크립트 또는 웹 대시보드 Train 화면
python -m trainer.train --difficulty beginner --episodes 100000

# 6) (다른 터미널) 프론트 (packages/web 기준)
cd ../packages/web
pnpm install && pnpm dev          # 기본 http://localhost:5173
```

---

## 9. Windows 특이사항 (네이티브 vs WSL2)
- **네이티브 Windows(가장 단순·권장)**: PyTorch CUDA 휠이 Windows를 직접 지원하므로 GPU 학습이 잘 된다. Postgres만 Docker Desktop 컨테이너로 띄우면 됨. 추가 GPU 설정 불필요.
- **WSL2**: 풀 Docker(GPU 포함)를 원하거나 리눅스 도구 일관성을 선호할 때 선택. 이 경우 WSL2 + NVIDIA Container Toolkit 설정이 필요. 본 가이드 기본 구성에서는 굳이 필요 없음.
- **노트북 4060이면**: 장시간 학습 시 발열로 클럭이 throttle될 수 있으니 충전 연결·쿨링 확보. 데스크톱이면 무관.

---

## 10. 학습 중 모니터링·튜닝
- `nvidia-smi -l 1` 로 GPU util·VRAM 실시간 관찰. 이 모델은 작아서 8GB 중 일부만 쓴다(배치를 키울 여유 많음).
- **GPU util이 낮은데 CPU가 바쁘면** 병목은 GPU가 아니라 환경 스텝(게임 로직, CPU)이다 → 환경 벡터화/병렬 워커로 처리량을 늘린다(기술 백서 §2.5 학습 파이프라인).
- 배치 크기·리플레이 용량을 4060 여유에 맞춰 키우면 처리량이 올라간다. `TrainConfig`(기술 백서 §6.3)에서 조정.

---

## 11. (참고) 풀 Docker + GPU로 가고 싶다면
- compose에 `server` 서비스를 추가하고 GPU 예약을 건다:
  ```yaml
  server:
    build: ./server
    env_file: .env
    depends_on:
      db: { condition: service_healthy }
    ports: ["8000:8000"]
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
  ```
- 이때 server 이미지에 CUDA torch를 포함해야 하고(베이스 이미지를 `nvidia/cuda` 또는 `pytorch/pytorch`로 교체), 호스트에 NVIDIA Container Toolkit(리눅스) 또는 WSL2 GPU 설정이 필요하다.
- **로컬 단독 개발이면 네이티브가 더 간단하므로, 기본 권장은 네이티브 유지**다. 풀 Docker는 추후 배포 표준화가 필요할 때 고려.
